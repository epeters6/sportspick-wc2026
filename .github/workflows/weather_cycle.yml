name: Weather Paper Research

on:
  schedule:
    # Fallback only; Supabase is the primary timed dispatcher (see ops runbook).
    - cron: '17 * * * *'
  workflow_dispatch:
    inputs:
      scheduled_for:
        description: 'UTC dispatch time from Supabase; leave blank for a manual run'
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: weather-paper-research
  cancel-in-progress: false

jobs:
  weather:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_KEY || secrets.SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      OWM_API_KEY: ${{ secrets.OWM_API_KEY }}
      APP_ENV: production
      LOG_LEVEL: INFO
      GITHUB_ACTIONS: "true"
      MODE: shadow
      LIVE_TRADING_ENABLED: "false"
      POLYMARKET_LIVE_ENABLED: "false"
      AUTO_BET_ENABLED: "0"
      POLY_AUTO_BET_ENABLED: "0"
      POLY_MLB_AUTO_BET_ENABLED: "0"
      POLY_MLB_INGAME_ENABLED: "0"
      DISCORD_WEBHOOK_URL: ""

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            pavlov/requirements.txt

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install -r pavlov/requirements.txt

      - name: Validate Supabase configuration
        run: python scripts/ci_check_env.py

      - name: Require a current trigger with enough time before the UTC boundary
        id: dispatch
        env:
          WEATHER_SCHEDULED_FOR: ${{ inputs.scheduled_for }}
        run: python scripts/check_weather_dispatch.py weather

      # The orchestrator isolates forecast failures from settlement and reporting.
      - name: Settle, forecast, evaluate, and report weather paper experiment
        if: steps.dispatch.outputs.execute == 'true'
        run: python -m scripts.run_weather_observed_cycle --report reports/weather/latest.json

      - name: Show separate paper performance and opportunity constraints
        if: always() && steps.dispatch.outputs.execute == 'true'
        run: |
          if [ -f reports/weather/performance.md ]; then
            cat reports/weather/performance.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Restore independent weather learning cache
        if: always() && steps.dispatch.outputs.execute == 'true'
        uses: actions/cache@v4
        with:
          path: |
            reports/weather_v3/cache
            reports/weather_v3/model
          key: weather-learning-v3-${{ runner.os }}-${{ hashFiles('backend/ml/weather_learning/**', 'scripts/run_weather_learning.py') }}-${{ github.run_id }}
          restore-keys: |
            weather-learning-v3-${{ runner.os }}-${{ hashFiles('backend/ml/weather_learning/**', 'scripts/run_weather_learning.py') }}-

      - name: Collect station weather and train independent research models
        if: always() && steps.dispatch.outputs.execute == 'true'
        continue-on-error: true
        timeout-minutes: 10
        env:
          OMP_NUM_THREADS: "1"
          OPENBLAS_NUM_THREADS: "1"
        run: python -m scripts.run_weather_learning --collect

      - name: Show weather model learning evidence
        if: always() && steps.dispatch.outputs.execute == 'true'
        run: |
          if [ -f reports/weather_v3/model/training.md ]; then
            cat reports/weather_v3/model/training.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload weather evidence even when a stage fails
        if: always() && steps.dispatch.outputs.execute == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: weather-paper-${{ github.run_id }}-${{ github.run_attempt }}
          if-no-files-found: warn
          retention-days: 90
          path: |
            reports/weather/**
            weather_shadow_decisions.jsonl
            weather_sync_status.json
            orderbook_snapshots.jsonl

      - name: Upload weather learning evidence
        if: always() && steps.dispatch.outputs.execute == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: weather-learning-${{ github.run_id }}-${{ github.run_attempt }}
          if-no-files-found: warn
          retention-days: 7
          path: |
            reports/weather_v3/latest.json
            reports/weather_v3/collection.json
            reports/weather_v3/snapshots.jsonl
            reports/weather_v3/market_capture.json
            reports/weather_v3/model/**
